<Page
    x:Class="Coordinates.UI.Views.MeasurementsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Template10.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Coordinates.UI.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:MeasurementsPageViewModel}">

    <Page.DataContext>
        <Binding Path="MeasurementsPageViewModel" Source="{StaticResource Locator}" x:Name="ViewModel"/>
    </Page.DataContext>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveVisualStateGroup">
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <controls:PageHeader x:Name="PageHeader" RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True" Text="Measurements"/>

        <!--  content  -->
        <Pivot x:Name="Pivot1" Grid.Row="1"
               RelativePanel.AlignLeftWithPanel="True"
               RelativePanel.AlignRightWithPanel="True"
               RelativePanel.AlignBottomWithPanel="True"
               RelativePanel.Below="PageHeader">

            <!--  origin  -->
            <PivotItem DataContext="{Binding CoordsOriginPartViewModel}" Header="Coordinates Origin">
                <ScrollViewer Margin="0,12,-12,0" HorizontalContentAlignment="Left"
                              VerticalScrollBarVisibility="Auto">
                    <RelativePanel HorizontalAlignment="Left">

                        <!--  controls here  -->
                        <ComboBox Name="ComboBox"
                                  ItemsSource="{Binding MeasurementTypes}"
                                  SelectedItem="{Binding SelectedMeasurementType}"
                                  SelectedValuePath="ComboBoxOption"
                                  DisplayMemberPath="MeasurementName"
                                  Header="Measurement type"
                                  PlaceholderText="Select measurement procedure"/>
                    </RelativePanel>

                </ScrollViewer>
            </PivotItem>

            <!--  computing  -->
            <PivotItem DataContext="{Binding CoordsComputationPartViewModel}" Header="Computation"
                       Padding="0">
                <ScrollViewer Margin="0,12,-12,0" VerticalScrollBarVisibility="Auto">
                    <RelativePanel HorizontalAlignment="Left">

                        <!--  controls here  -->
                        <TextBlock Margin="0,12,24,12" LineHeight="24"
                               TextWrapping="Wrap">
                            Also nothing... yet!
                        </TextBlock>
                    </RelativePanel>

                </ScrollViewer>
            </PivotItem>
        </Pivot>


    </RelativePanel>
</Page>
