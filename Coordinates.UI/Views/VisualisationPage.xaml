<Page xmlns:sfgrid="using:Syncfusion.UI.Xaml.Grid"  x:Class="Coordinates.UI.Views.VisualisationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:vm="using:Coordinates.UI.ViewModels"
    xmlns:sfchart="using:Syncfusion.UI.Xaml.Charts"
    xmlns:syncfusion="clr-namespace:Syncfusion.UI.Xaml.Charts;assembly=Syncfusion.SfChart.UWP"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance vm:VisualisationPageViewModel}">

    <Page.DataContext>
        <Binding Path="VisualisationPageViewModel" Source="{StaticResource Locator}" x:Name="ViewModel"/>
    </Page.DataContext>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <!-- change properties for narrow view  -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveVisualStateGroup">
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- change properties for narrow view  -->
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- change properties for normal view -->
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!-- change properties for wide view -->
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!--  header  -->
        <controls:PageHeader x:Name="PageHeader" RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True" Text="Wizualizacja"/>

        <sfchart:SfChart Name="XYChart" Header="Wykres x i y" RelativePanel.Below="PageHeader" Margin="12,12,0,0" Width="700" Height="300" FontSize="14">
            <sfchart:SfChart.PrimaryAxis>
                <sfchart:NumericalAxis Header="x" FontSize="14"></sfchart:NumericalAxis>
            </sfchart:SfChart.PrimaryAxis>
            <sfchart:SfChart.SecondaryAxis>
                <sfchart:NumericalAxis Header="y" FontSize="14"></sfchart:NumericalAxis>
            </sfchart:SfChart.SecondaryAxis>
            <sfchart:ScatterSeries ItemsSource="{Binding XYPicked}" XBindingPath="{Binding XPicked}" YBindingPath="{Binding YPicked}"/>
        </sfchart:SfChart>

        <sfgrid:SfDataGrid Name="DataGrid" AutoGenerateColumns="True" ItemsSource="{Binding XYZPicked}" 
                           RelativePanel.Below="XYChart" Margin="12,12,0,0" Height="300">
            <sfgrid:SfDataGrid.Columns>
                <sfgrid:GridTextColumn MappingName="XPicked.Name"/>
                <sfgrid:GridTextColumn MappingName="YPicked.Name"/>
                <sfgrid:GridTextColumn MappingName="ZPicked.Name"/>
            </sfgrid:SfDataGrid.Columns>
            <sfgrid:SfDataGrid.SortColumnDescriptions>
                <sfgrid:SortColumnDescription ColumnName="XPicked.Name"></sfgrid:SortColumnDescription>
            </sfgrid:SfDataGrid.SortColumnDescriptions>
        </sfgrid:SfDataGrid>

    </RelativePanel>
</Page>

